void searchNode(linkedList_h* H, int itdata){
//연결 리스트에서 특정 노드를 검색하여 반환하고자 하는 연산, itdata=200
  listNode* tempNode;
  tempNode = H → head'
  
  while(tempNode → data != itdat){
    tempNode = tempNode → link;
  }
  printf("Search successful!! \n:);
}
